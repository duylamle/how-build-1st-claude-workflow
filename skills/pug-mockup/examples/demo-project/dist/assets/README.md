# Assets

Place images, icons, fonts, and other static files here.

Reference them in Pug with relative paths:
```pug
img(src="assets/logo.png" alt="Logo")
img(src="assets/hero-bg.jpg" alt="Hero background")
```

These files are **not** processed by the Pug build — they stay as-is.
Copy them into this folder manually.
